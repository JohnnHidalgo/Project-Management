# Variables de entorno para producción - Frontend
# Copia este archivo a .env y configura los valores según tu entorno de producción

# API Base URL - use relative path so Nginx proxy handles backend
VITE_API_BASE_URL=/api